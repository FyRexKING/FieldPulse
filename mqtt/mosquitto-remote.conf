# Mosquitto configuration for remote MQTT broker (external client simulator)
# Plain MQTT for testing - no TLS

listener 1883
protocol mqtt
allow_anonymous true

persistence true
persistence_location /mosquitto/data/

log_dest file /mosquitto/log/mosquitto.log
log_dest stdout
log_type all
log_timestamp true

max_connections -1
message_size_limit 0
